[build-system]
requires = ["setuptools>=61", "wheel", "torch>=2.4"]
build-backend = "setuptools.build_meta"

[project]
name = "tma_dwconv"
version = "0.0.1"
description = "NHWC Depthwise Conv2D forward on Blackwell B200 (sm_100) with TMA + warp-specialized multi-stage pipeline."
requires-python = ">=3.9"
dependencies = ["torch>=2.4"]
